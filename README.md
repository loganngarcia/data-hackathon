# data-hackathon

Workspace for a data hackathon project. See `AGENTS.md` for event rules and API notes.

Cursor agent skills live under `.agents/skills/` (installed with `npx skills add`). `skills-lock.json` records installed sources.

Do not commit API tokens or team secrets. Use environment variables or local untracked files.
