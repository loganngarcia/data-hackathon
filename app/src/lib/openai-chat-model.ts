/**
 * Default for `POST /api/gemini` (OpenAI backend).
 * [GPT-5.4 nano](https://developers.openai.com/api/docs/models/gpt-5.4-nano) — text + image; fast / cost-efficient.
 * Override with `OPENAI_MODEL`.
 */
export const DEFAULT_OPENAI_CHAT_MODEL = "gpt-5.4-nano";
